{% extends 'base.html.twig' %}

{% block title %}AccountingMonth{% endblock %}

{% block body %}
    <h1>AccountingMonth</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ accounting_month.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ accounting_month.label }}</td>
            </tr>
            <tr>
                <th>StartAt</th>
                <td>{{ accounting_month.startAt ? accounting_month.startAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>EndAt</th>
                <td>{{ accounting_month.endAt ? accounting_month.endAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_accounting_month_index') }}">back to list</a>

    <a href="{{ path('app_accounting_month_edit', {'id': accounting_month.id}) }}">edit</a>

    {{ include('accounting_month/_delete_form.html.twig') }}
{% endblock %}
