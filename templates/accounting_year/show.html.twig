{% extends 'layouts/_main.html.twig' %}

{% block title %}AccountingYear{% endblock %}

{% block main %}
    <h1>AccountingYear</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ accounting_year.id }}</td>
            </tr>
            <tr>
                <th>StartAt</th>
                <td>{{ accounting_year.startAt ? accounting_year.startAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>EndAt</th>
                <td>{{ accounting_year.endAt ? accounting_year.endAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_accounting_year_index') }}">back to list</a>

    <a href="{{ path('app_accounting_year_edit', {'id': accounting_year.id}) }}">edit</a>

    {{ include('accounting_year/_delete_form.html.twig') }}
{% endblock %}
